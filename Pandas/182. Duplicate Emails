import pandas as pd

def duplicate_emails(person: pd.DataFrame) -> pd.DataFrame:
    duplicates = person[person["email"].duplicated(keep ='first')] 
    single_duplicates = duplicates["email"].drop_duplicates()
    output = pd.DataFrame(single_duplicates)
    return output
